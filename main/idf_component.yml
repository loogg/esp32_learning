## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: '>=4.1.0'
  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `public` flag can be set.
  #   # `public` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are public by default.
  #   public: true
  espressif/iot_usbh_rndis: ^0.2.0
  espressif/esp_lvgl_port: ^2.6.0
  lvgl/lvgl: ^8.4.0
  espressif/esp_io_expander_tca95xx_16bit: ^2.0.0
  espressif/console_simple_init: ^1.1.0
  espressif/console_cmd_ping: ^1.1.0
  espressif/console_cmd_ifconfig: ^1.0.1
  cherry-embedded/cherryusb: ^1.4.100
  espressif/usb_host_hid: ^1.0.3
  espressif/freetype: ^2.13.3~1
